# OpenS2S Real-time Streaming Requirements (Fixed for Dependency Conflicts)
# This version resolves the vllm/outlines/pyairports dependency conflict

# Core ML/AI packages with compatible versions
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0
transformers==4.45.2
tokenizers==0.20.0
accelerate==0.34.2

# Audio processing
librosa==0.10.1
soundfile==0.13.1
whisper==1.1.10
phonemizer==3.3.0
piper_phonemize==1.1.0

# Real-time streaming dependencies
fastapi==0.115.0
uvicorn==0.30.6
websockets==12.0
webrtcvad==2.0.10
python-multipart==0.0.9
aiofiles==24.1.0
psutil==5.9.8

# Scientific computing (compatible versions)
numpy==1.26.4
scipy==1.13.1
matplotlib==3.9.2
pandas==2.2.2

# ML utilities
datasets==2.21.0
safetensors==0.4.5
huggingface-hub==0.25.0
peft==0.12.0

# Configuration and utilities
omegaconf==2.3.0
hydra-core==1.3.2
PyYAML==6.0.2
rich==13.8.0
tqdm==4.66.5
packaging==24.1
requests==2.32.3

# Audio/Speech specific
conformer==0.3.2
einops==0.8.0
einops_exts==0.0.4

# Optional: Gradio for fallback interface (compatible version)
gradio==4.44.0

# Development and monitoring
wandb==0.17.8

# System utilities
setuptools==75.1.0
wheel==0.44.0
pip==24.2

# Flash attention (will be installed separately due to compilation requirements)
# flash_attn==2.7.0.post2

# Removed problematic packages:
# - vllm==0.6.1.post1 (causes outlines/pyairports conflict)
# - diffusers==0.32.2 (not needed for core functionality)
# - lightning==2.5.1 (not needed for inference)
# - modelscope==1.27.1 (not needed for core functionality)
# - natten==0.17.5 (not needed for core functionality)

# Additional packages that may be needed (install separately if required)
# dac==0.4.3
# k_diffusion==0.1.1.post1
# onnxruntime==1.22.0
# pyarrow==17.0.0
# ttsfrd==0.1.0
# Unidecode==1.3.8
# wget==3.2
# wids==0.1.11
# x_transformers==2.4.9
